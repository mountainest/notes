{
    /* 切换环境类型快捷键：Ctrl+Alt+E (Cmd + option + E) */
    "rest-client.environmentVariables": {
        "$shared": {},
        "local":{
            "host":"http://localhost:9000/rest/"
        }
    }
}